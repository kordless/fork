//! Fork community surface — placeholder.
//!
//! Intentionally minimal. The point of Fork is the verifiable history and
//! the ability to use it, not a new social network. Any future UI should
//! sit on top of search + get, not replace them.

fn main() {
    println!("Fork community surface is intentionally thin.");
    println!("Use `fork search` and `fork get` for active access to the preserved web.");
}
